@page
@model ExtraUserInfoModel
@{
    ViewData["Title"] = "Extera User Info";
}
<h3>User claims</h3>
<ul>

    @foreach (var claim in User.Claims)
    {
        <li>@claim.Type: @claim.Value</li>
    }
</ul>
